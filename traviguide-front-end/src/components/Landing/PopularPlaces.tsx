import { Box, Text } from "@chakra-ui/react";

const PopularPlaces = () => {
  return (
    <Box mx={10} mt={40}>
      <Text textColor={"#EAB875"} fontWeight={'bold'} fontSize={24}> Popular Destinations:</Text>
    </Box>
  );
};

export default PopularPlaces;
